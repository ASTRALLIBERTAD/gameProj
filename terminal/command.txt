[BUILD]
cargo ndk -t arm64-v8a -o path/to/output build --release
cargo build --release




let scene = load::<PackedScene>("res://Player/players.tscn");
        
        let instance = scene.instantiate().unwrap();
        self.base_mut().add_child(&instance);